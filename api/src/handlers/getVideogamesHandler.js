const getVideogamesController = require("../controllers/getVideogamesCotroller")

const getVideoGamesHandler = async () =>{
/*     try {
        const data = getVideogamesController()
        if(data){
            return res.status(200).json(data)
        }
    } catch (error) {
        console.log(error)
    } */
}

module.exports={getVideoGamesHandler}